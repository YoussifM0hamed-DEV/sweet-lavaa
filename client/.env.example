# API base URL.
#  - Single-service deployment (Express serves this app): leave as /api
#  - Separate frontend host: set the full URL, e.g. https://api.example.com/api
VITE_API_URL=/api

# Google OAuth client id (must match GOOGLE_CLIENT_ID on the server)
VITE_GOOGLE_CLIENT_ID=
